<?xml version="1.0" encoding="UTF-8" ?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><mapper namespace="com.example.sessionlogin.mappers.MemberMapper">  <insert id="insertMember" parameterType="com.example.sessionlogin.models.Member">    INSERT INTO members (username, password, email, status, is_deleted, created_at, updated_at)    VALUES (#{username}, #{password}, #{email}, #{status}, #{isDeleted}, NOW(), NOW())  </insert></mapper>